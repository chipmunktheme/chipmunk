{% set submit_page = get_option('submit_page') %}

<{{ submit_page ? "a href='#{submit_page | e('esc_url')}'" : 'button data-popup="submit"' }} class="{{ class }}">
  {{ __('Submit', 'chipmunk') }}
</{{ submit_page ? 'a' : 'button' }}>
