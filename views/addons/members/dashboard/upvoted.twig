{# TODO: Set proper DB key Chipmunk\Extensions\Upvotes::$db_key #}
{% set resources = get_resources({
  post_status: 'any, trash',
  perm: 'readable',
  meta_query: {
    relation: 'OR',

    upvotes: {
      key: '',
      value: user.id,
      compare: '=',
    },
  },
}) %}

{% include 'content/loop/resource.twig' %}
