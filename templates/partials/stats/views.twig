{% if is_option_enabled('views', post.type) %}
  <li class="{{ class('c-stats__item', 'views') }}" title="{{ __('Views', 'chipmunk') }}">
    {% include 'partials/icon.twig' with { icon: 'eye' } only %}
    {{ get_formatted_number(get_views(post.id)) }}
  </li>
{% endif %}
