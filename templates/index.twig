{% extends '_layout.twig' %}

{% block content %}
  {% if is_author %}
    {% include 'sections/author.twig' %}
  {% endif %}

  {# TODO: Find a way to get the proper post type of the archive #}
  {# {% include "content/archive/#{ queried_object.name }.twig" ignore missing %} #}
  {% include "content/archive/#{ (posts | first).post_type }.twig" ignore missing %}
{% endblock %}
