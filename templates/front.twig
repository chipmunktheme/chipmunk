{% extends '_layout.twig' %}

{% block content %}
  {% include 'sections/intro.twig' %}

  {% if is_option_enabled('listings', 'homepage', false) %}
    {% do action(get_theme_slug('before_resources_tabs')) %}
    {% include 'sections/tabs.twig' %}
    {% do action(get_theme_slug('after_resources_tabs')) %}

    {% include 'sections/toolbox.twig' %}
  {% endif %}

  {% if is_option_enabled('collections', 'homepage', false) %}
    {% include 'content/archive/collection.twig' %}
  {% endif %}

  {% if is_option_enabled('posts', 'homepage', false) %}
    {# {% include 'content/archive/post.twig' %} #}
  {% endif %}
{% endblock %}
